<%= @response.errors.full_messages.first if @response.errors.any? %>

<%= form_for([@escapade, @response ]) do |f| %>
<%= f.label :comment %>
<%= f.text_field :comment %>
<%= f.submit "Continue" %>
<% end %>
